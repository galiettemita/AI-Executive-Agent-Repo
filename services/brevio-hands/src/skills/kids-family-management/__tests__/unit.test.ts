import { describe, it } from 'node:test';
import assert from 'node:assert/strict';

describe('kids-family-management unit', () => {
  it('scaffold compiles', () => {
    assert.equal(1, 1);
  });
});
