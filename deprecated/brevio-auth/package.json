{
  "name": "@brevio/brevio-auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node dist/index.js",
    "lint": "eslint src --ext .ts",
    "test": "pnpm run build && node --test dist/**/*.test.js"
  }
}
