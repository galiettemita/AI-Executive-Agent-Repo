.DS_Store
.env
.env.local
.env.*.local

node_modules/
**/node_modules/

dist/
**/dist/
.turbo/
**/.turbo/

.cache/
.pnpm-store/

# Local-only files
.repo_locks/
storage/
artifacts/

# *.local.bak — created by 'sed -i.bak' edits on env files; same secrets as .local, must NEVER commit
.env.*.local.bak
