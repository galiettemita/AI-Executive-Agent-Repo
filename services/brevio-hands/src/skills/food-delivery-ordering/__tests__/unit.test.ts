import { describe, it } from 'node:test';
import assert from 'node:assert/strict';

describe('food-delivery-ordering unit', () => {
  it('scaffold compiles', () => {
    assert.equal(1, 1);
  });
});
