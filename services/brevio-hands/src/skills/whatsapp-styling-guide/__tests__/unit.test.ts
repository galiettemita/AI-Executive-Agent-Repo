import { describe, it } from 'node:test';
import assert from 'node:assert/strict';

describe('whatsapp-styling-guide unit', () => {
  it('scaffold compiles', () => {
    assert.equal(1, 1);
  });
});
