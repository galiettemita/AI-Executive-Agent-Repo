name: security-scan
on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * 1'

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash scripts/security/run_security_validation.sh
      - run: bash scripts/security/run_govulncheck.sh
