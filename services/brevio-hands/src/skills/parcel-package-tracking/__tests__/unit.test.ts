import { describe, it } from 'node:test';
import assert from 'node:assert/strict';

describe('parcel-package-tracking unit', () => {
  it('scaffold compiles', () => {
    assert.equal(1, 1);
  });
});
