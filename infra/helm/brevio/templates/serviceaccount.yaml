{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "brevio.serviceAccountName" . }}
  labels:
    {{- include "brevio.labels" . | nindent 4 }}
{{- end }}
