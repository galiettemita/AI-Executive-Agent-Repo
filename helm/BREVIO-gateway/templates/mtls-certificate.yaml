{{- if .Values.mtls.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: brevio-gateway-mtls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: brevio-gateway-mtls-tls
  duration: {{ .Values.mtls.duration | quote }}
  renewBefore: {{ .Values.mtls.renewBefore | quote }}
  privateKey:
    algorithm: ECDSA
    size: 256
  issuerRef:
    name: {{ .Values.mtls.issuerName }}
    kind: {{ .Values.mtls.issuerKind }}
  commonName: gateway.brevio.internal
  dnsNames:
    - gateway.brevio.internal
{{- end }}

