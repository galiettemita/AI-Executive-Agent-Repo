import { describe, it } from 'node:test';
import assert from 'node:assert/strict';

describe('restaurant-reservations unit', () => {
  it('scaffold compiles', () => {
    assert.equal(1, 1);
  });
});
