# REQUIRED
DATABASE_URL=postgres://user:password@localhost:5432/brevio
REDIS_URL=redis://localhost:6379
TEMPORAL_HOST=localhost:7233

# NEW — Reasoning upgrade (all optional; graceful degradation when absent)
# Enables: LLM planning, extended thinking, ReAct loop, Step-Back, GoT,
#          semantic critic, PRM, council deliberation, MCTS, self-consistency
ANTHROPIC_API_KEY=sk-ant-...

# Optional: OpenAI embeddings for System 1 semantic matching
OPENAI_API_KEY=sk-...

# Service config
SERVICE_VERSION=v1.0.0
ENVIRONMENT=production
LOG_LEVEL=info
