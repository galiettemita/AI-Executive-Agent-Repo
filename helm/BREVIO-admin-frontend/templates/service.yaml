apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name | lower }}
spec:
  selector:
    app: {{ .Chart.Name | lower }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
